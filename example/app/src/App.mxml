<?xml version="1.0" encoding="utf-8"?>
<s:Application xmlns:fx="http://ns.adobe.com/mxml/2009" 
			   xmlns:s="library://ns.adobe.com/flex/spark" minWidth="955" minHeight="600" initialize="initializeHandler(event)" creationComplete="creationCompleteHandler(event)">
	<fx:Script>
		<![CDATA[
			import mx.events.FlexEvent;
			
			protected function initializeHandler(event:FlexEvent):void
			{
				try
				{
					ExternalInterface.addCallback("test", function(text:String):void{
						lbl.text = text;
					});
				}
				catch(e:Error)
				{
					lbl.text = e.toString();
				}
			}
			
			protected function creationCompleteHandler(event:FlexEvent):void
			{
				ExternalInterface.call("$FlashWarp", ExternalInterface.objectID, "exec", ["test", "Hello JavaScript"]);	
			}		
		]]>
	</fx:Script>
	<s:Label id="lbl" />
</s:Application>
